import BN from 'bn.js';
export declare const split: (n: number, k: number, sec: string) => Promise<{
    x: BN;
    y: string;
}[]>;
export declare const join: (shares: {
    x: string;
    y: string;
}[]) => string;
