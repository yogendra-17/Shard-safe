/// <reference types="node" />
import { ConfigureParams, UploadParams, RecoveryParams } from "./types";
declare class RecoveryKit {
    private config;
    constructor();
    configure: (params: ConfigureParams) => void;
    private splitKey;
    private combineKey;
    private uploadKeyToDrive;
    private uploadKeyToAWS;
    private recoveryKeyFromDrive;
    private recoverKeyFromAWS;
    upload: (params: UploadParams) => Promise<Array<string>>;
    recovery: (params: RecoveryParams) => Promise<string>;
    phraseToEntropy: (mnemonic: string, wordlist?: string[]) => Promise<string>;
    entropyToPhrase: (entropy: string | Buffer, wordlist?: string[]) => Promise<string>;
}
export declare const RecoveryKitSingleton: RecoveryKit;
export {};
