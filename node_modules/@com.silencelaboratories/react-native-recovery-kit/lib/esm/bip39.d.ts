import { Buffer } from "buffer";
export declare const phraseToEntropy: (mnemonic: string, wordlist?: string[]) => Promise<string>;
export declare const entropyToPhrase: (entropy: string | Buffer, wordlist?: string[]) => Promise<string>;
